'use client';

import {
  ScheduleAppointmentModal,
  ScheduleAppointmentModalProps as AppointmentModalProps,
} from './ScheduleAppointmentModal';

export { ScheduleAppointmentModal, ScheduleAppointmentModal as AppointmentModal };
export type { AppointmentModalProps };
export default ScheduleAppointmentModal;
